;; JUP emacs

(require 'ert)
(require 'jup-test-helper)

(defun jup/test_print ()
  (message "something")
  )

(defclass something


(print (jup/test_print))

(defun test-jup-search-dirlist(dirlist dir)
  ""
  )


(ert-deftest test-jup-search-dirlist()
  (unwind-protect
      (progn
	(create-directory-environment-00)
	
	)
      (delete-directory-environment-00))
  )


